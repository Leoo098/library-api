# Checar porta:
netstat -aof | findstr 5433

# Create Network

docker network create library-network

# Rodar container Postgre na vesão 17.2
docker run --name librarydb -e POSTGRES_PASSWORD=postgres -e POSTGRES_USERNAME=postgres -e POSTGRES_DB=library -p 5432:5432 -d --network library-network postgres:17.2

# Rodar Pgadmin 4
docker run --name pgadmin4lib -e PGADMIN_DEFAULT_EMAIL=admin@admin.com -e PGADMIN_DEFAULT_PASSWORD=admin -p 15432:80 -d --network library-network dpage/pgadmin4:9.5
